# Ajax with React's Higher Order Components.

See `src/examples` for ajax and infinite scrolling wrappers

See `src/App.js` for how to invoke these wrappers in UI components.

# How to run:

Clone and `cd` into the repository.

```
npm install
npm run start
```
